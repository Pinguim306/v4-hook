# Reference material (not part of the deliverable)
.prism-ref/

# Foundry
contracts/out/
contracts/cache/
contracts/broadcast/
# Dependencies are pinned + reinstalled via contracts/bootstrap.sh (see CI)
contracts/lib/

# Sandbox-only solc-js shim deps (native solc used in CI)
contracts/node_modules/
contracts/package-lock.json

# Node / web
web/node_modules/
web/dist/
web/.vite/

# Misc
.DS_Store
*.log

# TS build cache
web/*.tsbuildinfo
web/tsconfig.tsbuildinfo

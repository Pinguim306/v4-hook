# Default profile — used in CI / real machines with a native solc.
# via_ir must stay OFF: solc 0.8.26's IR pipeline hits Yul stack-too-deep errors compiling
# v4-core's PoolManager (upstream Uniswap also builds without via-ir, optimizer only).
[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.26"
evm_version = "cancun"
optimizer = true
optimizer_runs = 800
via_ir = false
ffi = false
fs_permissions = [{ access = "read", path = "./" }]

[profile.default.fuzz]
runs = 256

# Sandbox profile — used where only the solc-js WASM compiler is reachable (no native solc).
# solc-js cannot compile the very large e2e compilation unit, so it is skipped here; the
# lightweight mock-based unit suite gives full logic coverage. Run with:
#   FOUNDRY_PROFILE=sandbox forge test --use ./solc-wrapper.js
[profile.sandbox]
src = "src"
out = "out"
libs = ["lib"]
evm_version = "cancun"
optimizer = false
via_ir = false
ffi = false
skip = ["test/e2e/**"]
fs_permissions = [{ access = "read", path = "./" }]

[profile.sandbox.fuzz]
runs = 64

[fmt]
line_length = 110
tab_width = 4
int_types = "long"

# See more config options https://github.com/foundry-rs/foundry/blob/master/crates/config/README.md#all-options
